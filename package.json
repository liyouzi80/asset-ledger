{
  "name": "asset-ledger",
  "private": true,
  "scripts": {
    "sync": "mkdir -p static/css && cp index.html static/ && cp css/*.css static/css/",
    "dev": "npm run sync && wrangler dev",
    "deploy": "npm run sync && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
